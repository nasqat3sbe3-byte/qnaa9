# نبض — فرع تطوير مستقل

- تطوير نبض فقط على `nabad-independent`، ومسار المحرك الحالي `watcher/`.
- قنص Render على `main` مستقل؛ ممنوع الدمج إلى `main` أو تعديل ملفات قنص.
- يجب ضبط خدمة Northflank لتتبع `nabad-independent` بدل `qanas-watcher-experiment` عند اعتماد الفرع.
- هذا فصل على مستوى الفروع، وليس مستودعين منفصلين. للفصل الكامل يُنشأ مستودع `nabad` مستقل وتُنقل إليه ملفات `watcher/` وتُغيَّر إعدادات Northflank.
- لا تُحذف ملفات قنص أو تُنقل أو تُسحب من فرع الإنتاج.
